div.seach
  = search_form_for q, url: url do |f|
    div.input-group
      = f.search_field :name_cont, class: 'form-control', placeholder: t('defaults.search_word')
      div.input-group-append
        = f.submit class:'btn btn-outline-light'
